const {_Node, LinkedList} = require('./linkedList')

class LinkedList {
    constructor() {
            this.head = null;
    }


const main = () => {
    class NewLinkedList {
    constructor() {
            this.head = null;
    }
 insertFirst(item){
        this.head = new _Node(item, this.head);
    }
}
